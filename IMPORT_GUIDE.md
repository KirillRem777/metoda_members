# 📊 Руководство по импорту участников из CSV

## Обзор функционала

Система импорта CSV позволяет:

✅ **Массово загружать** участников из CSV файла
✅ **Автоматически привязывать** фотографии по имени файла
✅ **Создавать пользователей** WordPress с паролями
✅ **Импортировать таксономии** (типы, роли, локации)
✅ **Обновлять существующие** записи участников

## 🚀 Быстрый старт

### Шаг 1: Подготовьте CSV файл

У вас уже есть файл `wordpress_members_import_final.csv` с правильной структурой!

**Обязательные колонки:**
- `post_title` - ФИО участника (например: "Иванов Иван Иванович")
- `member_position` - Должность
- `member_company` - Организация

**Опциональные колонки:**
- `member_email` - Email (нужен для создания пользователей)
- `member_phone` - Телефон
- `member_bio` - Биография/О себе
- `member_specialization` - Специализация
- `member_experience` - Опыт работы
- `member_interests` - Интересы
- `member_location` - Местоположение
- `taxonomy_member_type` - Тип (Эксперт/Участник)
- `taxonomy_member_roles` - Роли через `|` (например: "Эксперт|Куратор секции")
- `taxonomy_member_location` - Локация для таксономии

### Шаг 2: Подготовьте фотографии

1. Поместите все фотографии в папку `photos/`
2. Назовите файлы **точно так же** как ФИО:
   ```
   Аболмасов Алексей Владимирович.jpg
   Абрамова Виктория Викторовна.png
   ```
3. Поддерживаются форматы: JPG, PNG, JPEG

### Шаг 3: Запустите импорт

1. Откройте WordPress Admin
2. Перейдите в **Участники → Импорт CSV**
3. Заполните форму:
   - **CSV файл** - выберите ваш CSV
   - **Папка с фотографиями** - оставьте `photos`
   - **✅ Создать пользователей** - автоматически создаст аккаунты
   - **✅ Обновить существующие** - перезапишет данные при совпадении ФИО
4. Нажмите **"🚀 Начать импорт"**

### Шаг 4: Проверьте результат

После импорта вы увидите статистику:
- ✅ Создано участников
- 🔄 Обновлено участников
- 📷 Загружено фотографий
- 👤 Создано пользователей

## 📋 Формат данных

### Пример строки CSV

```csv
post_title,member_position,member_company,member_email,member_phone,taxonomy_member_type,taxonomy_member_roles
"Иванов Иван Иванович","Директор","ООО Компания","ivanov@example.com","+7 999 123-45-67","Эксперт","Эксперт|Куратор секции"
```

### Множественные значения в таксономиях

Используйте символ `|` для разделения:
```
"Эксперт|Куратор секции|Лидер проектной группы"
```

## 🔐 Создание пользователей

Если включена опция **"Создать пользователей"**:

1. **Username** генерируется автоматически из ФИО транслитерацией:
   - "Иванов Иван" → `ivanov_ivan`
   - Добавляется номер при дубликатах: `ivanov_ivan2`

2. **Пароль** генерируется случайно (12 символов)

3. **Роль** - автоматически `member`

4. **Первый вход** - пользователя попросят сменить пароль

⚠️ **Внимание:** Пароли НЕ сохраняются в системе после импорта. Используйте функцию **"Создать пользователей"** в админке для получения таблицы с логинами и паролями.

## 📸 Автоматическая привязка фотографий

Система ищет фотографии в следующем порядке:

1. `Иванов Иван Иванович.jpg`
2. `Иванов Иван Иванович.JPG`
3. `Иванов Иван Иванович.jpeg`
4. `Иванов Иван Иванович.JPEG`
5. `Иванов Иван Иванович.png`
6. `Иванов Иван Иванович.PNG`

Первый найденный файл будет загружен в медиатеку и привязан как главное фото.

## 🔄 Обновление существующих участников

При включении **"Обновить существующие"**:

- Система ищет участников по **точному совпадению ФИО**
- Обновляет ВСЕ поля (включая мета-данные и таксономии)
- НЕ удаляет существующие фотографии (только добавляет новые)

При выключенной опции - дубликаты будут пропущены с предупреждением.

## ⚠️ Возможные предупреждения

После импорта могут появиться предупреждения:

- **"Участник уже существует (пропущен)"** - включите "Обновить существующие"
- **"Фотография не найдена"** - проверьте имя файла и формат
- **"Email уже используется"** - этот email уже зарегистрирован в WordPress

## 🛠 Альтернативные методы импорта

### Вариант 1: Прямой импорт через SQL (для разработчиков)

```bash
# Подключиться к базе данных WordPress
wp db import members_backup.sql
```

### Вариант 2: Использование WP-CLI

```bash
wp post create --post_type=members --post_title="Иванов Иван" --post_status=publish
wp post meta add <POST_ID> member_position "Директор"
```

### Вариант 3: REST API (программный импорт)

```php
wp_remote_post(site_url('/wp-json/wp/v2/members'), [
    'body' => [
        'title' => 'Иванов Иван',
        'status' => 'publish',
        'meta' => [
            'member_position' => 'Директор'
        ]
    ]
]);
```

## 📊 Экспорт данных

Для экспорта текущих участников в CSV:

```bash
# Через WP-CLI
wp post list --post_type=members --format=csv > members_export.csv
```

Или используйте плагин **WP All Export** из админки WordPress.

## 🔍 Проверка импортированных данных

После импорта проверьте:

1. **Участники → Все участники** - список всех импортированных
2. **Пользователи** - созданные WordPress аккаунты
3. **Медиатека** - загруженные фотографии
4. **Таксономии**:
   - Участники → Типы участников
   - Участники → Роли участников
   - Участники → Локации

## 📝 Шаблон CSV

Скачайте шаблон: `wordpress_members_import_final.csv`

Или создайте новый с заголовками:

```csv
post_title,post_content,post_status,post_type,member_position,member_company,member_email,member_phone,member_bio,member_specialization,member_experience,member_interests,member_location,taxonomy_member_type,taxonomy_member_roles,taxonomy_member_location
```

## 💡 Советы и лучшие практики

1. **Сначала тестируйте** на 2-3 записях
2. **Делайте бэкап** базы данных перед массовым импортом
3. **Проверяйте кодировку** CSV файла (должна быть UTF-8)
4. **Не используйте** запятые в данных (используйте точку с запятой)
5. **Оборачивайте текст** в кавычки если есть специальные символы

## 🆘 Решение проблем

### Не импортируются кириллические символы

Убедитесь что CSV файл в кодировке **UTF-8 with BOM**.

Excel → Сохранить как → CSV UTF-8

### Фотографии не загружаются

1. Проверьте права на папку `photos/`: `chmod 755 photos`
2. Проверьте имена файлов (должны точно совпадать с ФИО)
3. Проверьте размер фото (макс. 8MB по умолчанию)

### Ошибка "Недостаточно памяти"

Увеличьте лимит в `php.ini`:
```ini
memory_limit = 256M
upload_max_filesize = 64M
post_max_size = 64M
```

### Таймаут при импорте

Импортируйте частями по 50-100 записей.

## 📞 Поддержка

Если возникли проблемы - проверьте:
1. Логи WordPress: `wp-content/debug.log`
2. Логи сервера: `/var/log/apache2/error.log`
3. Консоль браузера (F12) при импорте

---

**Готово!** Теперь вы можете импортировать участников быстро и безопасно. 🎉
